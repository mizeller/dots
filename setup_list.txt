
# - [x] vivaldi
# - [x] zed
# - [x] (logitech software to customize mouse scrolling direction)
# - [x] https://protonvpn.com/
# - [x] https://proton.me/drive/download
#   - [x] ln -s /Users/mizeller/Library/CloudStorage/ProtonDrive-mizeller@pm.me-folder ~/proton
#   - [x] ln -s ~/proton/projects ~/projects

# BREW
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

brew install --cask ghostty
brew install font-jetbrains-mono-nerd-font
brew install --cask nikitabobko/tap/aerospace
# don't forget to link the config file!
# ln -s ~/.config/aerospace/.aerospace.toml ~/.aerospace.toml

brew tap FelixKratz/formulae
brew install --cask font-sketchybar-app-font
brew install --cask sf-symbols
brew install sketchybar
brew install borders

# CLI tools
brew install neovim
brew install git
brew install starship   # cross-shell prompt
brew install eza        # modern alternative to ls
brew install fzf        # fuzzy finder
brew install ffmpeg     # video and audio processing
brew install tmux

# other tools
brew install --cask flameshot
brew install --cask mactex
brew install --cask librewolf
brew install --cask whatsapp
brew install --cask spotify
# brew install --cask raycast
brew install --cask vlc
brew install --cask kindavim



brew install docker
brew install docker-compose
brew install zsh-autosuggestions
brew install zsh-completions
brew install httpie			# CLI for http requests
brew install uv				# python env manager w/ RUST
brew install imagemagick		# CLI image transforms
brew install tree  			# list dir as tree
brew install watch 			# watch -n 0.2 <cmd> 
brew install black 			# python formatter
brew install --cask vscodium
brew install yazi sevenzip jq poppler fd ripgrep zoxide resvg font-symbols-only-nerd-font
brew install gitui

# to support swipe gestures between aerospace workspaces
curl -sSL https://raw.githubusercontent.com/acsandmann/aerospace-swipe/main/install.sh | bash

# cpp
brew install cmake

# rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# todotree rust crate; useful cli to highlight TODO/NOTE/... in codebase
cargo install todo-tree 
## add to $PATH
echo 'export PATH="$HOME/.cargo/bin:$PATH"' >> ~/.zshrc && source ~/.zshrc




